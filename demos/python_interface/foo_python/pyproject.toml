[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "foo_python"
version = "0.1.0"
description = "Generated gradgen Python wrapper for foo"
requires-python = ">=3.11"

[tool.maturin]
module-name = "foo"