# Composed Runner

This binary crate depends on the generated `composed_kernel` crate by path and
calls the staged primal and gradient kernels, printing their outputs.

Run it from this directory with:

```bash
cargo run
```
