# Single-Shooting Residual Penalty Runner

This binary crate calls the generated `single_shooting_penalty_kernel` crate
and prints the cost, rollout states, control-sequence gradient, and
Hessian-vector product for one sample input.
