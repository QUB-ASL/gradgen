[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "{{ crate_name }}"
version = "0.1.0"
description = "Generated gradgen crate with a PyO3 Python interface"
requires-python = ">=3.11"

[tool.maturin]
module-name = "{{ crate_name }}"
